# tGroup

```
cd $home && git clone https://github.com/sajjad-021/tGroup.git; cd tGroup; chmod 700 start.sh; ./start.sh -y --force-yes
cd data
nano config.lua
tmux new-session -s script "bash steady.sh -t"
```
